name: 'Terraform Destroy Manual'

on:
  workflow_dispatch:

# --- ADICIONE ESTA SEÇÃO ---
permissions:
  id-token: write
  contents: read
# -------------------------

<<<<<<< HEAD
  jobs:
    terraform:
      name: 'Terraform'
      # Condição: Só executa este job se o evento for um 'pull_request'
      runs-on: ubuntu-latest
=======
jobs:
  terraform-destroy:
    name: 'Terraform Destroy'
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.destroy == 'true'
    runs-on: ubuntu-latest
>>>>>>> origin/feature7

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          role-to-assume: arn:aws:iam::579375260812:role/GitHubActionRole-DatabaseInfra
          aws-region: us-east-1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Terraform Init
        run: terraform init

      - name: Terraform Destroy
        run: terraform destroy -auto-approve -var="db_password=${{ secrets.DB_PASSWORD }}"
